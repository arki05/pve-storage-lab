# Values tests read. Not switches: every test runs against every backend, and
# what this one cannot do is declared in expectations.toml with a reason.
#
# No CONSISTENCY_CHECK: a thin pool has no scrub. lvs reports metadata and data
# usage, not integrity, and there is no fsck to run against the pool itself.
