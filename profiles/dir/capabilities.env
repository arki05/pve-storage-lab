# Values tests read. Not switches: every test runs against every backend, and
# what this one cannot do is declared in expectations.toml with a reason.
#
# No CONSISTENCY_CHECK: the storage is a directory on the node's own root
# filesystem, so "is the storage undamaged" is a question about the node, not
# about anything this profile set up.
